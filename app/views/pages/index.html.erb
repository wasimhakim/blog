<div class="m-10">
  <div class="flex flex-col gap-4">
    <h1 class="font-bold text-green-600 text-4xl">Cool You are Logged In now!</h1>
    <p>You can now create your own page.</p>
    <%= link_to 'Create your page', new_page_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white font-medium w-2/12 text-center" %>
    <%= button_to('Logout', destroy_user_session_path, method: :delete) %>
  </div>

  <div id="pages" class="min-w-full">
    <%= render @pages if current_user.admin? %>
  </div>
</div>
